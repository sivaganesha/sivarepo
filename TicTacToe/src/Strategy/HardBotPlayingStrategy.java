package Strategy;

import model.Board;
import model.Move;

public class HardBotPlayingStrategy implements BotPlayingStrategy{
    @Override
    public Move makeMove(Board p) {
        return null;
    }
}
