package Strategy;

import model.Board;
import model.Move;

public class MediumBotPlayingStrategy implements BotPlayingStrategy{
    @Override
    public Move makeMove(Board p) {
        return null;
    }
}
